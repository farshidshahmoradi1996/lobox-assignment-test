version: '3.6'
services:
  lobox-app:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 5173:5173
    command: ['npm', 'run', 'dev']
